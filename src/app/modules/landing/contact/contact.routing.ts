import { Route } from '@angular/router';
import { LandingContactComponent } from 'app/modules/landing/contact/contact.component';

export const landingContactRoutes: Route[] = [
    {
        path     : '',
        component: LandingContactComponent
    }
];
